---
import RootLayout from '../layouts/RootLayout.astro';
import { buttonVariants } from '../components/ui/Button';
import { getCollection } from 'astro:content';
import ActivityGraph from '../components/ActivityGraph.astro';
import { Linkedin, Mail } from 'lucide-react';

const activities = await getCollection('activities');
const buddyBox = await getCollection('buddyBox');
const buddyBoxData = buddyBox[0]?.data;
---

<RootLayout title="Eric Gu" description="Projects, cooking, and more">
  <div class="page-container-home">
    <section class="mb-16 text-center">
      <div class="mx-auto max-w-2xl">
        <h1 class="text-3xl sm:text-4xl font-bold mb-3 sm:mb-4 gradient-text">Eric Gu</h1>

        <div class="flex justify-center gap-4 mb-4">
          <a href="https://www.linkedin.com/in/eric-t-gu/" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-foreground" aria-label="LinkedIn">
            <Linkedin className="size-7" aria-hidden="true" />
          </a>
          <a href="mailto:eric.gu@berkeley.edu" class="text-muted-foreground hover:text-foreground" aria-label="Email">
            <Mail className="size-7" aria-hidden="true" />
          </a>
        </div>

        <div class="text-base text-muted-foreground leading-relaxed mb-6 space-y-1">
          <p>Mechanical Engineering + Electrical Engineering & Computer Science</p>
          <p><em>University of California, Berkeley</em>, '28</p>
          <p>I enjoy all things mechatronics, including robots, drones, and 3D printers! I also love cooking, biking, speedcubing, and run a pretty extensive homelab.</p>
        </div>

        <div class="flex flex-wrap justify-center gap-3 sm:gap-4">
          <a href="/projects" class={buttonVariants({ variant: 'project' })}>Projects</a>
          <a href="/cooking" class={buttonVariants({ variant: 'outline' })}>Cooking</a>
          <a href="/Gu_Eric_Resume_5_1_2026.pdf" target="_blank" rel="noopener noreferrer" class={buttonVariants({ variant: 'outline' })}>Resume</a>
        </div>
      </div>
    </section>

    <div class="separator-gradient mb-12"></div>

    <section>
      <h2 class="section-title">What I'm Involved In</h2>
      <div class="mx-auto w-full max-w-[36rem]">
        <ActivityGraph activities={activities} />
      </div>
    </section>

    <div class="separator-gradient my-12"></div>

    <section>
      {buddyBoxData ? (
        <>
          <div class="flex flex-wrap justify-center items-center gap-4 sm:gap-5">
            {buddyBoxData.items.map((item) => (
              <a
                href={item.link}
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center justify-center transition-opacity hover:opacity-90"
                aria-label={item.alt}
                title={item.alt}
              >
                <div class="w-[158px] sm:w-[176px]" style="aspect-ratio: 88 / 31;">
                  <img
                    src={item.image}
                    alt={item.alt}
                    style={{ imageRendering: 'pixelated', borderRadius: '0' }}
                    width="176"
                    height="62"
                    loading="lazy"
                    decoding="async"
                    sizes="(max-width: 640px) 158px, 176px"
                    class="w-full h-full object-contain"
                  />
                </div>
              </a>
            ))}
          </div>
        </>
      ) : (
        <p class="text-muted-foreground">Add buddy box content in src/content/buddy-box/index.md</p>
      )}
    </section>
  </div>
</RootLayout>
